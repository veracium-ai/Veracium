PACKAGE: 0022-0023-v10 — external ROUND 10
SEALED:  __TS__ (UTC)
COMMIT:  __COMMIT_FULL__   (the same commit COLLECTED.txt names)
MEASURED: __MEASURED__

0004 is not a candidate: approved at round 2, frozen on W1-W8.

BOTH ROUND-8 BLOCKERS ARE FOLDED, and both are the same disease at different
depths: a number or a status maintained BY HAND beside one produced by a
machine.

  R8-1 — `raised` was read with `.get(..., [])`, so FORGETTING the field was
  indistinguishable from declaring no findings: you injected a verdict naming
  R99-1 with no `raised` and the gate reported nothing. A returned verdict must
  now DECLARE, `raised=[]` included, and omission raises. The displayed count
  comes from `raised` rather than the legacy `findings=`, which disagrees in
  four rows — that field is now documented as what the round REPORT stated
  (historically blocking-only, or grouped differently for a coupled round)
  rather than silently contradicting the column above it. Two regressions: a
  missing field, and the rendered count against the structure.

  R8-2 — four package claims were typed. The harness line said 17/17 against
  an 18/18 executable; "All 31" evidence commands described a 33-row ledger of
  which the test runs 32; COLLECTED claimed the suite was measured with itself
  present when the sealer measures BEFORE building it; and the inventory said
  git-dependent tests skipped for want of `.git` when sealing measures the git
  checkout and the sealed reconciliation shows no such skips.

  All four are now produced rather than asserted: the sealer RUNS both
  harnesses and substitutes their output, derives the evidence split from the
  ledger, and the two-phase workflow is described as what it is.

ROUND 9 (R9-1) THEN CAUGHT US OVERSTATING THE EXTRACTION CHECK, and the code
moved to meet the claim rather than the claim shrinking to meet the code.

  Both carriers said the sealer reran "both harnesses and both verifiers from
  the EXTRACTED archive". It ran the two harnesses and nothing else; the
  verifiers ran BEFORE the archive existed, against the build tree. You traced
  the subprocesses and found it.

  The claim was the better one — the build tree is not the artifact, which is
  the whole reason to extract — so all six checks now run FROM THE EXTRACTION:
  both harnesses, verify_collected against the sealed -rs, reconcile,
  render_closure --check and render_operation --check. They come from a single
  registry in the sealer, the carrier's list is GENERATED from that registry,
  and the sealer aborts if it executes anything other than exactly it. The two
  cannot drift apart again, which is the failure that produced this finding.

WHAT THE PREVIOUS ROUND FIXED, still standing:

  R7-1 — our "completeness gate" compared SETS OF IDS. Set equality cannot see
  a wrong round, an erased evidence string, a duplicate row, or a count that
  disagrees with the rows it counts, and you demonstrated all four by mutation.
  Finding identities are STRUCTURAL now: each verdict row in reviews.py
  declares `raised=[...]`, and the ledger must match on (spec, kind, round, id)
  EXACTLY — extras, duplicates, wrong rounds and empty evidence each fail the
  build. We re-ran your mutations; each is caught by name.

  Building it exposed three data defects it then caught: 18 wrong round labels,
  a finding attributed to two rounds because a later verdict REFERENCED it
  rather than raising it, and the cross-spec stripper eating `0022 R99-1` —
  which you predicted. Counts are DERIVED: the ledger headers now read "10 for
  0023; 31 across the pair" where prose had said 26.

  AND FOUR EVIDENCE COMMANDS DID NOT RUN. Three said `python3 -m pytest` and a
  bare python3 has no pytest; one was a grep-for-absence rendered INTO the
  document it searched, so the file supplied its own needle and the command
  failed on a clean tree. Evidence that does not execute is a description of
  evidence. They all run now — parameterised on `$PY`, the launcher's venv —
  and a test RUNS THEM. **The exact split is in COLLECTED.txt and is DERIVED,
  not stated here**: round 8 caught this paragraph saying "All 31" about a
  33-row ledger of which the test runs 32, the launcher being run separately
  during sealing because it builds a venv and runs the whole suite.

  R7-2 — this carrier still described the SQLite-floor launcher a round after
  the code changed, and repeated v6's "1656 passed, 8 skipped" against a
  different test set. The description is taken from the script that exists, and
  THE SEALER NOW RUNS THE LAUNCHER on the final tree and substitutes what it
  printed. A number nobody measured in this state can no longer reach the
  carrier.

STILL OPEN: the store-side R19 binding remains enforcement-BEFORE-
implementation. It becomes behavioural the day the store code exists, which
needs 0022 accepted.
