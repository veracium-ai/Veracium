0001 generated-content trust class — round-__ROUND__ external-review package (__VERSION__)
=====================================================================================
package built:   __TS__ (UTC)
source commit:   __COMMIT__   (PACKAGE_MANIFEST.txt names the same one; the
                 sealer REFUSES if they differ)
__CANDIDATES__
                 Rounds 1-2 of this line (v2 reviewed 2026-07-31, v3 reviewed
                 2026-08-01) were document-only sends predating the sealer;
                 this is the line's FIRST SEALED package and the first live
                 seal under the C-plus header construction (the record you
                 can verify: collected_header.json, whole-file recompute via
                 `python specs/verify_extracted.py header`).

WHAT MOVED SINCE THE v3 REVIEW — your one blocking amendment, closed in code:
  The render marker keyed on `use_only` with hardcoded third-party text.
  Shipped 2026-08-15: `graph._ORIGIN_LABELS` keys on the AUTHOR, an
  unlabelled class fails safe to `unverified-origin`, and
  tests/test_render_origin.py trips on any new class without a label.
  v4 additionally: rewrites the v2-form carriers §13 missed (§4, §3b, §6
  I1/I2 — the appended-correction defect class), re-executes every
  mechanical claim after eleven releases of drift, answers the five specs
  that shipped author-consumers since v3 (§2d — the 0003 ladder's
  pre-provisioned assistant rung ratified, the 0005 import cap, 0022, the
  0024 co-edit sequencing, 0025, the MCP fail-closed map), and adds I11
  (fail-closed disclosure) + I12 (pair-keyed origin labels).

MEASUREMENT CONTEXT — GENERATED from the captured runtime probe
(RUNTIME_PROBE.json ships; the record derives from the file):
                 __CONTEXT__

Spec-Requires (derived from the spec headers, not restated):
                 __REQUIRES__

REPRODUCTION — a launcher, and it REFUSES rather than reporting:
  bash specs/evidence/offline/run_offline.sh
  LAUNCHER RESULT ON THIS PACKAGE'S FINAL STATE: __LAUNCHER__
  (complete transcript + exit status ship as LAUNCHER_TRANSCRIPT.txt,
  digest-bound in collected_header.json)

measured line:   __MEASURED__ (exit 0)
                 Measured FIRST, in the committed tree at the commit above;
                 this file is built from that run afterwards. Verified from
                 the EXTRACTED archive before staging — the list below is
                 GENERATED from the sealer's registry and the sealer aborts
                 if it runs anything else:
                 __EXTRACTED__
                 The full -rs output ships as COLLECTED_pytest_rs.txt and the
                 inventory below is COMPUTED FROM IT.

harnesses:       __HARNESSES__
                 (the standing store-level harnesses; run by the sealer on
                 the tree being sealed and re-run from the extraction)

evidence:        __EVIDENCE__

ENVIRONMENT-CONDITIONAL SKIP INVENTORY — GENERATED from specs/skip_inventory.py:
